server:
  port: 8762

eureka:
  instance:
    hostname: localhost
  server:
      port: 8761
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.server.port}/eureka/

spring:
  application:
    name: eurka-client